services:
  - type: web
    name: vessel-management-api
    runtime: docker
    plan: free
    dockerfilePath: ./docker/api.Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: WEB_ORIGIN
        sync: false
